1)
create table public.category (
  id bigint generated by default as identity not null,
  name text not null,
  constraint category_pkey primary key (id)
) TABLESPACE pg_default;

2)
create table public.conversation_participants (
  id uuid not null default gen_random_uuid (),
  conversation_id uuid null,
  user_id uuid null,
  joined_at timestamp with time zone null default now(),
  last_read_at timestamp with time zone null,
  role public.conversation_role not null default 'member'::conversation_role,
  constraint conversation_participants_pkey primary key (id),
  constraint conversation_participants_conversation_id_fkey foreign KEY (conversation_id) references conversations (id) on delete CASCADE,
  constraint conversation_participants_user_id_fkey foreign KEY (user_id) references auth.users (id) on delete CASCADE
) TABLESPACE pg_default;

create unique INDEX IF not exists one_owner_per_conversation on public.conversation_participants using btree (conversation_id) TABLESPACE pg_default
where
  (role = 'owner'::conversation_role);

create unique INDEX IF not exists conversation_participants_unique_user on public.conversation_participants using btree (conversation_id, user_id) TABLESPACE pg_default;

3)
create table public.conversation_system_events (
  id bigint generated by default as identity not null,
  conversation_id uuid not null,
  actor_id uuid null,
  event_type text not null,
  body text not null,
  meta jsonb null,
  created_at timestamp with time zone not null default now(),
  constraint conversation_system_events_pkey primary key (id),
  constraint conversation_system_events_actor_id_fkey foreign KEY (actor_id) references auth.users (id) on delete set null,
  constraint conversation_system_events_conversation_id_fkey foreign KEY (conversation_id) references conversations (id) on delete CASCADE,
  constraint cse_type_check check (
    (
      event_type = any (
        array[
          'created'::text,
          'pinned'::text,
          'unpinned'::text,
          'title_changed'::text,
          'participant_added'::text,
          'participant_left'::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

create index IF not exists idx_cse_convo_created on public.conversation_system_events using btree (conversation_id, created_at desc) TABLESPACE pg_default;

4)
create table public.conversations (
  id uuid not null default gen_random_uuid (),
  title text null,
  created_by uuid null,
  created_at timestamp with time zone null default now(),
  updated_at timestamp with time zone null default now(),
  avatar_url text null,
  constraint conversations_pkey primary key (id),
  constraint conversations_created_by_fkey foreign KEY (created_by) references auth.users (id) on delete set null
) TABLESPACE pg_default;

5)
create table public.event_photos (
  id bigint generated by default as identity not null,
  event_id bigint not null,
  photo_url text null,
  constraint event_photos_pkey primary key (id),
  constraint event_photos_event_id_fkey foreign KEY (event_id) references events (id)
) TABLESPACE pg_default;

6)
create table public.events (
  id bigint generated by default as identity not null,
  title text null,
  description text null,
  date_time_event timestamp without time zone null,
  address text null,
  organizer text null,
  price integer null,
  is_online boolean null default false,
  is_free boolean null default false,
  user_id uuid null,
  "selectCategory" text[] null,
  created_at timestamp with time zone null default CURRENT_TIMESTAMP,
  is_published boolean not null default true,
  status text null default 'pending'::text,
  moderation_score smallint null default '0'::smallint,
  moderation_flags jsonb null,
  constraint events_pkey primary key (id),
  constraint events_user_id_fkey foreign KEY (user_id) references users (id)
) TABLESPACE pg_default;

7)
create table public.favorites (
  user_id uuid not null,
  event_id bigint not null,
  created_at timestamp with time zone not null default now(),
  constraint favorites_pkey primary key (user_id, event_id),
  constraint favorites_event_id_fkey foreign KEY (event_id) references events (id) on delete CASCADE,
  constraint favorites_user_id_fkey foreign KEY (user_id) references auth.users (id) on delete CASCADE
) TABLESPACE pg_default;

8)
create table public.friendships (
  requester_id uuid not null,
  addressee_id uuid not null,
  status text not null default 'pending'::text,
  created_at timestamp with time zone not null default now(),
  updated_at timestamp with time zone not null default now(),
  constraint friendships_pkey primary key (requester_id, addressee_id),
  constraint friendships_addressee_fkey foreign KEY (addressee_id) references auth.users (id) on delete CASCADE,
  constraint friendships_requester_fkey foreign KEY (requester_id) references auth.users (id) on delete CASCADE,
  constraint friendships_no_self check ((requester_id <> addressee_id)),
  constraint friendships_status_check check (
    (
      status = any (array['pending'::text, 'accepted'::text])
    )
  )
) TABLESPACE pg_default;

create index IF not exists friendships_addressee_idx on public.friendships using btree (addressee_id) TABLESPACE pg_default;

create index IF not exists friendships_requester_idx on public.friendships using btree (requester_id) TABLESPACE pg_default;

create trigger trg_friendships_updated_at BEFORE
update on friendships for EACH row
execute FUNCTION set_updated_at ();

create trigger trg_guard_friendships_update BEFORE
update on friendships for EACH row
execute FUNCTION guard_friendships_update ();

9)
create table public.message_reactions (
  id bigint generated by default as identity not null,
  message_id bigint not null,
  user_id uuid not null,
  reaction text not null,
  created_at timestamp with time zone null default now(),
  constraint message_reactions_pkey primary key (id),
  constraint unique_user_reaction unique (message_id, user_id),
  constraint message_reactions_message_id_fkey foreign KEY (message_id) references messages (id) on delete CASCADE,
  constraint message_reactions_user_id_fkey foreign KEY (user_id) references users (id) on delete CASCADE,
  constraint reaction_allowed check (
    (
      reaction = any (
        array[
          '❤️'::text,
          '🫡'::text,
          '👌'::text,
          '👍'::text,
          '😡'::text,
          '🥲'::text,
          '😭'::text
        ]
      )
    )
  )
) TABLESPACE pg_default;

10)
create table public.messages (
  id bigint generated by default as identity not null,
  sender_id uuid not null,
  receiver_id uuid null,
  body text not null,
  created_at timestamp with time zone not null default now(),
  read_at timestamp with time zone null,
  conversation_id uuid null,
  message_type text null default 'text'::text,
  file_url text null,
  file_name text null,
  file_size bigint null,
  file_type text null,
  constraint messages_pkey primary key (id),
  constraint messages_receiver_fkey foreign KEY (receiver_id) references auth.users (id) on delete CASCADE,
  constraint messages_sender_fkey foreign KEY (sender_id) references auth.users (id) on delete CASCADE,
  constraint messages_conversation_id_fkey foreign KEY (conversation_id) references conversations (id) on delete CASCADE,
  constraint messages_no_self check ((sender_id <> receiver_id)),
  constraint messages_dm_or_conversation_check check (
    (
      (
        (conversation_id is not null)
        and (receiver_id is null)
      )
      or (
        (conversation_id is null)
        and (receiver_id is not null)
      )
    )
  ),
  constraint messages_body_len check (
    (
      (length(body) >= 1)
      and (length(body) <= 5000)
    )
  )
) TABLESPACE pg_default;

create index IF not exists messages_sender_idx on public.messages using btree (sender_id, created_at desc) TABLESPACE pg_default;

create index IF not exists messages_receiver_idx on public.messages using btree (receiver_id, created_at desc) TABLESPACE pg_default;

create index IF not exists messages_pair_idx on public.messages using btree (sender_id, receiver_id, created_at desc) TABLESPACE pg_default;

create index IF not exists messages_unread_idx on public.messages using btree (receiver_id, sender_id, created_at desc) TABLESPACE pg_default
where
  (read_at is null);

create index IF not exists idx_messages_conversation_id_created_at on public.messages using btree (conversation_id, created_at desc) TABLESPACE pg_default;

create trigger trg_guard_messages_update BEFORE
update on messages for EACH row
execute FUNCTION guard_messages_update ();

create trigger trg_touch_conversation_updated_at
after INSERT on messages for EACH row when (new.conversation_id is not null)
execute FUNCTION touch_conversation_updated_at ();

11)
create table public.typing (
  user_id uuid not null,
  other_id uuid not null,
  is_typing boolean null default false,
  updated_at timestamp with time zone null default now(),
  constraint typing_pkey primary key (user_id, other_id)
) TABLESPACE pg_default;

12)
create table public.user_telegram (
  user_id uuid not null,
  telegram_id bigint not null,
  username text null,
  first_name text null,
  last_name text null,
  photo_url text null,
  auth_date bigint null,
  linked_at timestamp with time zone not null default now(),
  constraint user_telegram_pkey primary key (user_id),
  constraint user_telegram_telegram_id_key unique (telegram_id),
  constraint user_telegram_user_id_fkey foreign KEY (user_id) references auth.users (id) on delete CASCADE
) TABLESPACE pg_default;

13)
create table public.users (
  id uuid not null,
  created_at timestamp with time zone not null default now(),
  first_name text null,
  last_name text null,
  birth_day text null,
  phone text null,
  email text null,
  gender text null,
  image_path text null default 'https://zzxibxrgnkkzodmbsglc.supabase.co/storage/v1/object/public/Biom/ProfileImage/Default_image.jpg'::text,
  "It_business" boolean null default false,
  interests text[] null default '{}'::text[],
  username text null,
  description text null,
  constraint users_pkey primary key (id),
  constraint users_id_fkey foreign KEY (id) references auth.users (id) on delete CASCADE,
  constraint users_username_format check (
    (
      (username is null)
      or (
        (
          (length(username) >= 3)
          and (length(username) <= 20)
        )
        and (username ~ '^[a-zA-Z0-9_]+$'::text)
      )
    )
  )
) TABLESPACE pg_default;

create unique INDEX IF not exists users_username_unique on public.users using btree (lower(username)) TABLESPACE pg_default
where
  (
    (username is not null)
    and (username <> ''::text)
  );

create unique INDEX IF not exists users_username_lower_uniq on public.users using btree (lower(username)) TABLESPACE pg_default;
